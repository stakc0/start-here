[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "npm install",
  "python3 -m pip install --user -U yt-dlp"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
